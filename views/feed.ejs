<%- include('partials/header-logged') -%>
  <div class="mt-4 mx-12 mb-20">
    <div class="flex flex-col items-center mt-[36px]">
      <h2 class="font-['caveat'] text-6xl mb-4">doodlebook</h2>
      <ul class="grid grid-cols-3 gap-4 mt-5">
        <% for(var i=0; i<posts.length; i++) {%>
          <li class="flex justify-center">
            <a href="/post/<%= posts[i]._id%>">
              <img class="w-fit" src="<%= posts[i].image%>">
            </a>
          </li>
        <% } %>
        </ul>
    </div>
  </div>  
